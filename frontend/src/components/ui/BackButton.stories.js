import BackButton from './BackButton.vue'

export default { title: 'UI/BackButton', component: BackButton }

export const Default = { render: () => ({ components: { BackButton }, template: '<BackButton aria-label="Volver a bares">← Bares</BackButton>' }) }
